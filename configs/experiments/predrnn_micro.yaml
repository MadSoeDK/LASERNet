# PredRNN with combined loss for microstructure
# Spatiotemporal LSTM with zigzag memory flow for capturing temporal dynamics
network: predrnn_large
field_type: microstructure
loss: mse
seq_len: 3
max_epochs: 100
num_workers: 4
learning_rate: 1e-3
batch_size: 16
