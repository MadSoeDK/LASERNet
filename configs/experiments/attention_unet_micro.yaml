# Attention U-Net with combined loss for microstructure
# U-Net with attention gates for focused prediction on solidification regions
network: attention_unet_deep
field_type: microstructure
loss: mse
seq_len: 3
max_epochs: 100
num_workers: 4
learning_rate: 1e-3
batch_size: 16
