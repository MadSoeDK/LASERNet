# PredRNN with 3-level encoder (8x spatial reduction)
# Maximum grain-scale detail preservation with minimal compression
network: predrnn_shallow3l
field_type: microstructure
loss: mse
seq_len: 3
max_epochs: 100
num_workers: 4
learning_rate: 1e-3
batch_size: 16
